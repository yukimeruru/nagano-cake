<div class='container'>
  <h3>商品一覧</h3>
  <% @items.each do |item| %>
  <div>
    <%= attachment_image_tag item, :image, size: "300x300" %>
    <%= item.name %>
    <%= item.price %>
  </div>
  <% end %>
</div>